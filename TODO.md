implment the ask ai I need a good RAG this is biggest part ned to have good way to.

Implement the Audio using Eleven Labs adn it shodul sync well with good voice pleae

I want SEO please good SEO for every single page i want to get a strong presence. adn have a function to generate open graph image tha tis well beautiful and styled and good design pleae do a good job. now this generated open graph image will be generate in teh github flow for deployting this on my server but before it build if there i s a new blog it will generate the open graph, then write save it then build commit so the github bot will write a commit to sae this newly. instead of doing this on github workflow, i should manually to gnerate the open graph so a script hat checks if ther is no open graph for an artile it shoudl geerate

I need a way that I can set a DOPEY SUBSCRIPTION ID so this use would not use the name generator and the avatar genertors insead use dopey avatrt an the name Dopey, I will set this to my ID when i subscribe to my blog so i can chat with user and they know me. see the database and set an env variable so it efault to it

Send email on Subscription, send emails when someone replies to your comment
or a reply you replied to

I wil use resend API for the mail integration

I need a good way I can manage to notify my entire email list when I post new stuff or just want to reach out to them good way to mail them, implement newsletter just liek blogs, so once we deploy when runing production build there be a github action to publish newletter, so when i write a newsletter there is a github job to publish if there's a new newletter it will publish it and update the state in the mdx too

TO manage it all myself but pretty good for unsubscription there should be page to handle unsubscription

I need cool stuff like reveal and image
make the design move with parrallax effect

I havne't implement mail and new letter unsubscription all those things i havne't implemented elevenlabs audio, translation and RAG AI, parralax effect better SEO and more beautiful rendering of og
